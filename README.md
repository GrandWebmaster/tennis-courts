# Свободные корты

Страница с расписанием свободных теннисных кортов Городского теннисного центра
«Проспект» в Рыбинске. Видно, что свободно в ближайшие десять дней, клик по номеру
корта ведёт на бронь.

Открывается тут: https://grandwebmaster.github.io/tennis-courts/

## Как устроено

Сайт статический и во время просмотра никуда не ходит: страница читает только
свой `data.json`, лежащий рядом. Данные в этот файл кладёт GitHub Action.

```
build/config.json   корты, услуги, тарифные окна, праздники
build/fetch.py      собирает data.json из открытого API онлайн-записи центра
data.json           результат сборки, обновляется автоматически
index.html          страница
assets/             стили и рендер сетки
```

Раз в пятнадцать минут workflow запускает `build/fetch.py`, тот опрашивает десять
дней и переписывает `data.json`. Если свободное время не изменилось, коммита нет —
незачем плодить пустую историю.

## Откуда берутся слоты

Онлайн-запись центра отдаёт свободное время через открытый ajax-эндпоинт. Запрос без
идентификатора мастера возвращает сразу все корты, поэтому на день хватает одного
обращения, а десять дней собираются параллельно за пару секунд.

Длительность считается на нашей стороне. Сервис отдаёт получасовки; час свободен там,
где свободны две смежные получасовки, полтора часа — три. Мы сверили это с ответами
сервиса для часовой и полуторачасовой аренды на живых данных: совпадает точно.

Цена зависит от времени и дня недели: под каждое тарифное окно у центра заведена
отдельная услуга. Окна перечислены в `build/config.json`, из них берётся и цена
в подсказке, и ссылка на бронь нужной услуги. Полуторачасовой аренды в будни
на 07:00–08:00 и после 21:00 в прайсе нет, такие слоты идут без цены.

## Запустить у себя

```bash
python3 build/fetch.py
python3 -m http.server 8000
```

Зависимостей нет, только стандартная библиотека Python 3.

## Оговорки

Данные не мгновенные. Между обновлениями проходит четверть часа, и за это время слот
могли занять. Возраст расписания показан над таблицей; если он вырос — значит
обновление не отработало, и на странице появится предупреждение.

Праздники перечислены датами в `build/config.json` и правятся руками раз в год:
переносы выходных утверждаются постановлением, из календаря их не вычислить.

Расписание неофициальное. Итоговое время и цену подтверждает центр при записи.
